# Round 3: Translate the LAST remaining untranslated strings

Same rules as round 2. Every string gets a real `translation_en`.
Preserve control tags (`<LINE>`, `<PAGE>`, `<CLOSE>`, etc).
These are the FINAL missing strings — mostly NPC dialogue, character
intro text, and short exchanges that were missed by earlier extraction.

Input files in this directory:
- E0.json (2,780 strings)
- E1.json (431 strings)
- E2.json (1,448 strings)
- ADV.json (14 strings)
- S2D.json (79 strings)

Output: same files with `translation_en` filled for every entry.
